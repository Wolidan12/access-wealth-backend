#!/usr/bin/env node
// Destructive legacy plan resets are disabled. Existing users with previously
// activated plans must remain eligible for daily earnings.
console.error('Legacy plan reset is disabled. Existing active and legacy plans are preserved.');
process.exit(0);
